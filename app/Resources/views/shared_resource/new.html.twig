{% extends "::base.html.twig" %}

{% block content %}
    {{ form_start(form, {'action': path('app_sharedresource_new')}) }}
        {{ form_errors(form) }}
        {{ form_rest(form) }}
        <button type="submit"><i class="fa fa-check">OK</i></button>
    {{ form_end(form) }}
{% endblock %}
